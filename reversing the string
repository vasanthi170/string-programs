#reversing a string
s   = 'python'
rev = ''
for ch in s:
    rev = ch + rev
print(rev)


#reversing a string using range
s   = 'python'
rev = ''
for ind in range(0,len(s)):
    rev = s[ind] + rev
print(rev)


#reversing a string using range
s   = 'python'
rev = ''
for ind in range(len(s)-1,-1,-1):
    rev =  rev + s[ind]
print(rev)
